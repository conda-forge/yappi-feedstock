{% set version = "1.7.5" %}

package:
  name: yappi
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/y/yappi/yappi-{{ version }}.tar.gz
  sha256: 4f9176339ba33b7c1d0a83fd9422e2dffdd19b5bda3e7b0d13fc0e68c25589df

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vvv
  entry_points:
    - yappi = yappi:main

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
  host:
    - pip
    - python
    - setuptools >=44
  run:
    - python

test:
  requires:
    - pip
  imports:
    - yappi
  commands:
    - pip check
    - yappi --help


about:
  home: https://github.com/sumerc/yappi
  license: MIT
  license_file: LICENSE
  summary: Yet Another Python Profiler
  license_family: MIT

extra:
  recipe-maintainers:
    - nicoddemus
